# mv 移动与重命名命令卡

## 适用场景
- 文件重命名或目录迁移。

## 用户常见问法
- 把这个文件改名。
- 把日志移动到归档目录。

## 推荐命令
mv old.log new.log
mv *.log /data/archive/
mv /data/a /data/b

## 参数解释
- mv 源 目标: 同目录下即重命名，跨目录即移动。

## 风险与回滚
- 会改变原路径，建议先用 echo 预演批量操作。
