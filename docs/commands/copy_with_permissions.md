# cp 复制命令卡

## 适用场景
- 复制文件或目录并尽量保留属性。

## 用户常见问法
- 复制这个目录到备份路径。
- 保留时间戳和权限复制。

## 推荐命令
cp file.txt /data/backup/
cp -r ./config ./config.bak
cp -a /data/app /data/app_bak

## 参数解释
- -r: 递归复制目录。
- -a: 尽可能保留属性（归档模式）。

## 风险与回滚
- 会写入目标路径，覆盖风险可先加 -i 交互确认。
